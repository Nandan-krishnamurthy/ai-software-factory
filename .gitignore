# Factory-local state (active target pointer) — never committed
.factory-local/

# Claude Code per-machine settings (e.g. additionalDirectories for the target)
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
